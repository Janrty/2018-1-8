<div id="a1"><h1>全局配置</h1> <p>Vue.config 是一个对象，包含 Vue 的全局配置。可以在启动应用之前修改下列属性：</div><br /><div id="a2"> #<strong>silent</strong></p> <ul> <li>类型：Function</li> <li>默认值：Function</li> <li>用法： <blockquote> <p>段落引用Vue.config.errorHandler = function (err, vm, info) {<br /> // handle error<br /> // <code>info</code> 是 Vue 特定的错误信息，比如错误所在的生命周期钩子<br /> // 只在 2.2.0+ 可用<br /> }</p> </blockquote> </li> </ul> <p></div><div id="a3">#<strong>optionMergeStrategies</strong></p> <ul> <li>类型：Function</li> <li>默认值：Function</li> <li>用法： <blockquote> <p>段落引用Vue.config.errorHandler = function (err, vm, info) {<br /> // handle error<br /> // <code>info</code> 是 Vue 特定的错误信息，比如错误所在的生命周期钩子<br /> // 只在 2.2.0+ 可用<br /> }</p> </blockquote> </li> </ul> <table> <thead> <tr> <th style="text-align:left">标题1</th> <th style="text-align:center">标题2</th> <th style="text-align:right">标题3</th> </tr> </thead> <tbody> <tr> <td style="text-align:left">左对齐</td> <td style="text-align:center">居中</td> <td style="text-align:right">右对齐</td> </tr> <tr> <td style="text-align:left">----------------------</td> <td style="text-align:center">-------------</td> <td style="text-align:right">-----------------</td> </tr> </tbody> </table> <h2>脚注(footnote)</h2> </div><div id="a4"><h1>全局API</h1> <p>Vue.config 是一个对象，包含 Vue 的全局配置。可以在启动应用之前修改下列属性：</div><br /> <div id="a5">#<strong>Vue.extend</strong></p> <ul> <li>类型：Function</li> <li>默认值：Function</li> <li>用法： <blockquote> <p>段落引用Vue.config.errorHandler = function (err, vm, info) {<br /> // handle error<br /> // <code>info</code> 是 Vue 特定的错误信息，比如错误所在的生命周期钩子<br /> // 只在 2.2.0+ 可用<br /> }</p> </blockquote> </li> </ul></div> <div id="a6"><p>#<strong>Vue.nextTick</strong></p> <ul> <li>类型：Function</li> <li>默认值：Function</li> <li>用法： <blockquote> <p>段落引用Vue.config.errorHandler = function (err, vm, info) {<br /> // handle error<br /> // <code>info</code> 是 Vue 特定的错误信息，比如错误所在的生命周期钩子<br /> // 只在 2.2.0+ 可用<br /> }</p> </blockquote> </li> </ul> <table> <thead> <tr> <th style="text-align:left">标题1</th> <th style="text-align:center">标题2</th> <th style="text-align:right">标题3</th> </tr> </thead> <tbody> <tr> <td style="text-align:left">左对齐</td> <td style="text-align:center">居中</td> <td style="text-align:right">右对齐</td> </tr> <tr> <td style="text-align:left">----------------------</td> <td style="text-align:center">-------------</td> <td style="text-align:right">-----------------</td> </tr> </tbody> </table></div>